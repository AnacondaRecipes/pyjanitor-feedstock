{% set name = "pyjanitor" %}
{% set version = "0.32.23" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 9570b1768bb11d9964d2f46d3206793d04e4a367065178d95786caeb987f75d3

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  skip: true  # [py<38]

requirements:
  host:
    - pip
    - python
    - wheel
    - setuptools >=61.0
  run:
    - python
    - natsort >=8.4.0,<9
    - pandas-flavor >=0.8.1,<0.9
    - multipledispatch
    - scipy
    - janitor-rs >=0.6.1,<0.7
  run_constrained:
    - isort >=4.3.18
    - ipython >7.31.1

test:
  source_files:
    - tests
  imports:
    - janitor
    - janitor.accessors
  commands:
    - pip check
    - pytest -v tests
  requires:
    - pip
    - pytest >=3.4.2
    - pytest-xdist
    - hypothesis >=4.4.0
    - ipython >7.31.1
    - isort >=4.3.18

about:
  home: https://github.com/pyjanitor-devs/pyjanitor
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Tools for cleaning pandas DataFrames
  doc_url: http://pyjanitor.readthedocs.io
  dev_url: https://github.com/pyjanitor-devs/pyjanitor

extra:
  recipe-maintainers:
    - ericmjl
    - ocefpaf
